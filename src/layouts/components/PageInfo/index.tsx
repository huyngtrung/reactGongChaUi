import classNames from 'classnames/bind';
import styles from './HomeHeader.module.scss';

const cx = classNames.bind(styles);

const PageInfo = () => {
    return <div className={cx('wrapper')}>PageInfo</div>;
};
export default PageInfo;
